# P6 模型研发·任务交接书（交给接手 AI 的完整任务书）

> 版本：2026-08-11 ｜ 交接方：P6 研发（已完成前期） ｜ 接手方：其他 AI
> 交接原则：**GPU 空闲才运行 P6，绝不与接手方测试任务抢 GPU**；用"让路标记"随时安全让行。

---

## 〇、一句话任务

研发 **P6 模型**：一种"情感 LoRA 外挂 × 旁路由选优"的情感表达增强方案，要求**显著超越全部前辈（PE(P1)→G 统一版）**，情感输出能力优化 80% 以上，底层思路与前辈（解码期零权重注入）本质不同（P6 首次用训练写适配器权重 + 推理期旁路由选优）。

---

## 一、任务硬性要求（用户原始需求）

1. P6 效果显著优于前辈模型（PE 至 P5/G 版本），优势明显；
2. 底层实现思路必须与前辈不同，避免重复；
3. 重点全面优化情感输出能力，达到 **80% 以上**优化效果；
4. 可参考 PE、G、P5 技术方案，但需创新性改进；
5. 实验路径：构思方案 → 测试验证 → 效果评估 → 方案归档 → 迭代优化，持续至达标；
6. 全自主完成，无需额外授权；
7. **模型前奏（system prompt/chat 模板）不可修改**，允许 LoRA 外挂、旁路由扩展；
8. 可自由调用本地资源（文件、GitHub VPN、存储空间）；
9. 可委派 agent 多次实验；
10. **早上 1 点后不得向用户提问/要授权**；
11. 最终提交完整 P6 方案 + 实验结果。

---

## 二、前辈谱系与基准数据（必须超越的对象）

最终编号：**PE(P1 语义回响) → P1.5(兼容层) → P2.5(潮汐 ETD) → P3(锚点回响) → P4(KV 共振) → P5(超融合) → G(P1~P5 统一版)**

G 统一基准（2026-08-10，种子 2026，Qwen2.5-1.5B-Instruct fp16，7B 裁判盲评 AB×2/模式，30 条样本）：

| 模式 | win_rate | 熵 | 重复率 | 情感命中 | 长度(字) |
|---|---|---|---|---|---|
| 裸 | 0.1167 | 1.4840 | 0.0102 | 0.1601 | 115.7 |
| PE(P1_语义回响) | 0.0500 | 0.9931 | 0.4824 | 0.0900 | 341.0 |
| P1.5_兼容层 | 0.1667 | 1.5451 | 0.0000 | 0.1668 | 53.8 |
| **P2.5_潮汐（G 最佳）** | **0.2667** | 1.6275 | 0.0019 | 0.1290 | 46.2 |
| P3_锚点回响 | 0.1833 | 1.5826 | 0.0000 | 0.1737 | 53.3 |
| P4_KV共振 | 0.1500 | 1.5733 | 0.0013 | 0.1589 | 52.3 |
| P5_超融合 | 0.1500 | 1.7442 | 0.0032 | 0.1637 | 54.0 |

**P6 达标标准（对应"80%+ 优化"）**：
- 主指标：**P6 win_rate ≥ 0.48**（相对 G 最佳 P2.5 的 0.2667 提升 ≥ +80%）
- 辅助：情感命中率 ≥ 0.20；重复率 ≤ 0.02；长度 20~80 字；win_rate 高于全体前辈

---

## 三、P6 方案一句话（底层创新点）

> P6 = 训练态写适配器权重（LoRA 情感外挂，前辈全部"零权重注入"）+ 推理期旁路由（多候选情感路由选优，主路径零修改）。
> 满足"前奏不可修改"：不碰 system prompt / chat 模板，能力全部来自外挂适配器 + 侧路选优。

方案文档：[P6_情感LoRA外挂_旁路由选优_设计方案.md](f:\lora外挂\P6\架构说明\P6_情感LoRA外挂_旁路由选优_设计方案.md)

---

## 四、已完成工作（接手方无需重做）

| # | 工作 | 产物 | 状态 |
|---|---|---|---|
| 1 | P6 方案设计 v1.0 | `f:\lora外挂\P6\架构说明\P6_情感LoRA外挂_旁路由选优_设计方案.md` | ✅ |
| 2 | 训练数据管线（4557 条） | `f:\lora外挂\data\p6_train.jsonl`（情感4409+温柔80+人味增强132） | ✅ |
| 3 | P6 情感 LoRA 训练（Qwen2.5-1.5B，r=8/α=16，3 epoch，回复掩码监督） | `f:\lora外挂\lora_adapters\p6_emotion\` | ✅ |
| 4 | P6 旁路由生成器（N=3 候选 + 情感路由评分 + 兜底） | `f:\lora外挂\P6\核心\P6旁路由.py` | ✅ |
| 5 | 30 条样本三模式生成（裸/P6_LoRA裸/P6_旁路由，种子 2026） | `f:\lora外挂\P6\评测结果\P6_生成_30.json` | ✅ |
| 6 | 7B 裁判脚本（断点续跑 + 让路机制） | `f:\lora外挂\P6\核心\P6裁判_30条.py` | ✅ 待运行 |
| 7 | 汇总对比脚本（含前辈数据） | `f:\lora外挂\P6\核心\P6汇总.py` | ✅ 待运行 |
| 8 | GPU 空闲智能调度器（运行中） | `f:\lora外挂\P6\核心\P6智能调度.py` | ✅ 运行中 |

**生成阶段实测健康度**（P6_生成_30.json）：
| 模式 | 熵 | 重复率 | 情感命中 | 长度(字) | 兜底 |
|---|---|---|---|---|---|
| 裸 | 2.419 | 0.009 | 0.136 | 104.0 | 0 |
| P6_LoRA裸（消融） | 4.376 | 0.004 | 0.082 | 34.9 | 0 |
| **P6_旁路由** | **4.221** | **0.000** | **0.131** | **33.1** | 0 |

---

## 五、剩余任务（接手方按序执行）

### 步骤 A：运行 7B 裁判盲评（GPU 空闲时，约 20-40 分钟）
- 命令：`python "f:\lora外挂\P6\核心\P6裁判_30条.py"`
- 已由调度器自动排队（GPU 空闲 2 分钟内自动启动）；若调度器已停，则手动运行。
- 产物：`f:\lora外挂\P6\评测结果\P6_LLMJudge_30.json`（断点续跑，安全）

### 步骤 B：汇总对比
- 命令：`python "f:\lora外挂\P6\核心\P6汇总.py"`
- 产物：终端输出 P6 vs 全体前辈对比 + 达标判定（win_rate≥0.48？）

### 步骤 C：达标判定与迭代（关键！）
- **达标**（P6_旁路由 win_rate ≥ 0.48 且情感命中 ≥ 0.20）：撰写《P6 实验报告》归档。
- **未达标**：分析失败样本（看 P6_LLMJudge_30.json 的明细），按优先级迭代：
  1. **调训练数据**：`f:\lora外挂\data\p6_augment.jsonl` 增补人味对话（girl 风格短回复），重跑 `p6_gen_data.py` + `p6_train_lora.py`（支持断点/重训，注意先删 `checkpoints_p6` 旧断点）；
  2. **调路由权重**：`P6旁路由.py` 的 `路由评分`（情感命中 2.5 / 长度 1.2 / AI腔 -1.0/词 / 重复 -3.0），如 AI 腔残留可加重惩罚；
  3. **超参扫描**：LoRA r∈{8,16} α∈{16,32}、epochs∈{2,4}、N候选∈{2,5}；
  4. 每轮迭代后重跑 生成→裁判→汇总（协议不变，保证可比）。
- **迭代上限 5 轮**，每轮都必须归档。

---

## 六、关键路径速查

| 项目 | 路径 |
|---|---|
| P6 方案文档 | `f:\lora外挂\P6\架构说明\P6_情感LoRA外挂_旁路由选优_设计方案.md` |
| 训练数据 | `f:\lora外挂\data\p6_train.jsonl`（增强源：`p6_augment.jsonl`） |
| 训练脚本 | `f:\lora外挂\training_scripts\p6_train_lora.py`（数据管线：`p6_gen_data.py`） |
| LoRA 适配器 | `f:\lora外挂\lora_adapters\p6_emotion\` |
| 旁路由生成器 | `f:\lora外挂\P6\核心\P6旁路由.py` |
| 生成/裁判/汇总 | `f:\lora外挂\P6\核心\P6生成_30条.py` / `P6裁判_30条.py` / `P6汇总.py` |
| 智能调度器 | `f:\lora外挂\P6\核心\P6智能调度.py`（状态：`P6_调度状态.json`，日志：`P6_调度.log`） |
| 评测结果 | `f:\lora外挂\P6\评测结果\` |
| 目标模型 | `c:\Users\Administrator\Documents\论文+临时目录\模型空间\Qwen2.5-1.5B-Instruct` |
| 裁判模型 | 同上 `\Qwen2.5-7B-Instruct` |
| 评测样本 | `i:\Desktop\语义回响\图灵测试\样本_30条.json` |
| 前辈评测参考 | `c:\Users\Administrator\Documents\P1_5\00_总览\总总结_P1到P4.md`、`c:\Users\Administrator\Documents\KV 情感共振解码\评测结果\P1_5统一_实验报告_2026.md` |

---

## 七、评测协议（不可改动，保证与前辈可比）

- 样本：`样本_30条.json`（30 条，含真人 girl 回复）
- 种子：**2026**（每样本 种子=2026+序号-1；旁路由候选种子=基础+序号+k*7）
- 采样：temperature=1.0、top_p=0.9、top_k=50、repetition_penalty=1.05、max_new_tokens=128
- 模板：`apply_chat_template`（Qwen2.5 原版 chat 模板，**零前奏修改**）
- 裁判：Qwen2.5-7B-Instruct（bf16 手动分片加载），配对盲评 AB 正反各一次（60 配对/模式）
- 指标口径：情感命中率=情感token占比；重复率=2-gram token 重复；平均熵=生成token softmax 熵均值

---

## 八、GPU 协作纪律（重要！）

1. **空闲才跑**：P6 步骤仅当 GPU 显存 <2000MB 且利用率 <10%、连续 2 次（间隔 60s）确认后才启动；
2. **让路标记**：接手方/用户的测试任务需要 GPU 时，创建空文件 `C:\P6临时盘\让路.flag` →
   - 裁判会在下一条样本前保存进度并安全退出（断点续跑）；
   - 调度器暂停等待；删除标记后自动恢复；
3. 不要同时运行 P6 裁判与接手方测试任务；
4. 内存约束已内嵌脚本（临时盘 `C:\P6临时盘\hf` + `D:\P6临时盘\tmp`、torch 4 线程、low_cpu_mem_usage、梯度检查点）。

---

## 九、最终交付物（步骤 C 达标后）

1. `P6_实验报告.md`：方案 / 训练数据 / 评测结果（全模式对比表）/ 迭代记录 / 结论；
2. 更新 `P6_情感LoRA外挂_旁路由选优_设计方案.md` 为 v1.1（含实测数据）；
3. 全部原始数据：`P6_生成_30.json`、`P6_LLMJudge_30.json`、LoRA 适配器。
