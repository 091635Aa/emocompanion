# Checklist — 一体化全流程 AI 应用（build-integrated-fullflow-app）

## 项目骨架与一键启动
- [x] `j:\最后版本！` 建立分层目录（配置/核心引擎/前端/数据/测试/发行版）
- [x] `启动.py` 可一键启动：依赖检查 → FastAPI 服务 → 自动打开浏览器
- [x] `requirements.txt` 完整且含镜像源注释

## 硬件检测与设备适配
- [x] 正确检测 CUDA 可用性/版本/GPU 型号/显存总量与可用量/系统内存
- [x] 按模型+量化档位预估推理/微调显存需求，判定可运行性
- [x] AMD 环境（无 CUDA）明确提示"不支持 AMD"
- [x] 缺失依赖时给出安装命令引导

## 配置系统
- [x] 全局配置 JSON 覆盖所有模块输入/输出/格式/时间自定义项
- [x] Web 配置页可编辑并生效

## 数据预处理
- [x] 支持视频/音频/文本三种输入上传与清洗
- [x] ASR 调用全模态模型完成音频→文本（含时间戳，支持断点续转）
- [x] 按话题分割片段（非按时长），前端可预览与微调边界

## 打标引擎
- [x] 复用 f:\打标 能力自动打标（情感/内容/风格）
- [x] 支持人工复核
- [x] 可导出微调数据包（JSON/JSONL），可一键送入微调

## 日记生成
- [x] 与本地大模型（30B 千问等，可配置）多轮对话
- [x] 从 7 岁起生成超长期连续日记（每篇数百~数千字），结合打标数据与音视频画面
- [x] 支持人工审阅/修改；生成参数可配置
- [x] 日记落盘且可作为微调数据源（结构化、无乱码）

## 身份微调可行性
- [x] 完成小样本身份微调实验并输出《身份微调可行性报告.md》
- [x] 身份维度默认建议（推荐/实验性）已按结论设置

## 微调引擎
- [x] 自动识别模型架构与思考能力，选择正确 chat 模板/训练策略
- [x] 按显存自动选择 LoRA（fp16）或 QLoRA（4bit），可手动覆盖
- [x] 三维度（情感/记忆/身份）均可勾选，混合数据训练并记录占比
- [x] 文本结构化、无乱码（统一编码 + 清洗控制字符）
- [x] 训练日志/断点/产物落盘到 `数据\微调输出\`
- [x] 支持"一角色一模型"与"三维合一单模型"；单模型多记忆有"不推荐"标注 + 警告弹窗

## 模型管理
- [x] 本地模型扫描并登记（参数量/架构/量化）
- [x] 魔搭等镜像源在线下载（断点续传、进度回调）
- [x] 性能评估输出报告（显存/速度/能否推理/能否微调）

## 推理架构
- [x] V1 架构（语义回响简单注入）可生成
- [x] V 通用架构（回响+LoRA+RAG+记忆四层）按需激活、优雅回退
- [x] λ/γ/τ 自动推荐或用户自定义
- [x] 记忆外挂跨会话生效
- [x] 可加载微调产出模型，推理输出指标（语义熵/重复率/情感命中率/耗时/显存）

## 达标评估
- [x] 微调后运行图灵测试式达标判定
- [x] 输出《达标报告.md》（得分/均分/差距分析）

## Web 前端
- [x] 9 个页面齐全：仪表盘/预处理/打标/日记/微调/模型管理/推理/达标/配置
- [x] 微调页三维度可勾选并提示本次组合
- [x] 实验性选项（单模型多记忆）有警告弹窗确认

## 全流程编排与测试
- [x] 编排器支持模块独立运行与串联一键执行（步骤可勾选跳过）
- [x] 冒烟测试通过（文本 → 数据包 → 微调 → 推理）
- [x] 全流程测试通过并输出测试报告（视频/音频 → 分割 → ASR → 打标 → 日记 → 微调 → 达标 → 推理）

## 发行版
- [x] 打包脚本产出（依赖清单/启动器/配置模板/README）
- [x] GitHub 发布指引完成
- [x] 新目录解压后按 README 可启动
