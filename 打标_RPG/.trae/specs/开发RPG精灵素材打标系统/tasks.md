# Tasks

- [x] 任务 1：项目骨架与配置
  - [x] 1.1 创建中文目录树（数据层/业务逻辑层/接口层 + 配置/日志/进度）
  - [x] 1.2 编写 `配置\系统配置.json`（模型服务地址、并行数、重试、嵌入模型、路径映射）
  - [x] 1.3 编写 `业务逻辑层\初始化模块.py`（目录自动创建 + 默认配置落盘）

- [x] 任务 2：素材规格表模块
  - [x] 2.1 编写 `业务逻辑层\素材规格.py`：MV/MZ 素材类型注册表（characters/faces/tilesets A1-A5、B-E/sv_actors/sv_enemies），字段含类型、尺寸、行列布局、文件名前缀语义、自动图块布局
  - [x] 2.2 实现按实际尺寸 + 文件名前缀自动匹配规格；不确定时返回候选并输出警告
  - [x] 2.3 支持配置中按文件手动覆盖规格

- [x] 任务 3：素材自动分割模块
  - [x] 3.1 编写 `业务逻辑层\素材分割.py`：读取源素材目录，按规格切割为单格 PNG
  - [x] 3.2 角色图按 8 角色 12 帧 / 单角色 12 帧切割；头像按 8 格；图块按 48x48 网格；A1-A4 自动块按 2x3/2x2 整块切
  - [x] 3.3 输出切割坐标清单 JSON（源文件、行、列、像素区域、序号）+ 断点续切
  - [x] 3.4 空块（全透明）跳过并记录，避免无效打标

- [x] 任务 4：提示词工程与结果解析
  - [x] 4.1 编写 `业务逻辑层\提示词工程.py`：图像打标系统提示词 + 分析提示词（要求输出严格 JSON）
  - [x] 4.2 编写 `业务逻辑层\结果解析.py`：JSON 提取/清洗/schema 校验，解析失败自动重试（换种子）

- [x] 任务 5：推理引擎适配（复用 v1.0 的 30B 模型服务）
  - [x] 5.1 编写 `业务逻辑层\推理引擎.py`：连接 `http://127.0.0.1:8766/v1/chat/completions`，图片以 base64 `image_url` 传入
  - [x] 5.2 服务健康检查 / 看门狗重启 / 流式读取与超时处理
  - [x] 5.3 服务不可用时给出明确错误提示（提示先启动 f:\打标 的 30B 服务）

- [ ] 任务 6：打标流水线
  - [ ] 6.1 编写 `业务逻辑层\打标流水线.py`：读分割清单 → 逐格打标 → 结果写入 `数据层\打标结果`
  - [ ] 6.2 进度文件（每格状态）+ 断点续打
  - [ ] 6.3 多线程并行（默认 2）+ 单格失败重试 3 次 + 失败清单归档
  - [ ] 6.4 日志（时间戳/状态/耗时/错误）

- [ ] 任务 7：向量索引
  - [ ] 7.1 编写 `业务逻辑层\向量索引.py`：本地嵌入模型（默认 bge-small-zh-v1.5，CPU）加载与文本嵌入
  - [ ] 7.2 接入 ChromaDB（持久化到 `数据层\向量库`），写入（类型+描述+标签）向量与元数据
  - [ ] 7.3 支持全量重建索引与增量追加；生成缩略图（Pillow，可选）

- [ ] 任务 8：MCP 查询服务
  - [ ] 8.1 编写 `接口层\mcp_server.py`（官方 `mcp` SDK，stdio）：search_assets / get_asset / list_assets / asset_stats
  - [ ] 8.2 搜索结果紧凑一行式（素材ID、名称、类型、一句话描述、标签），详情接口返回完整 JSON
  - [ ] 8.3 提供 `启动MCP.py` 或 `python -m` 入口

- [ ] 任务 9：CLI 查询入口
  - [ ] 9.1 编写 `查询.py`：search / detail / list / stats 子命令，输出紧凑或详情格式

- [ ] 任务 10：汇总报告与端到端自测
  - [ ] 10.1 编写 `业务逻辑层\报告生成.py`：JSON + 简单 HTML 汇总（素材数/类型分布/成功率/失败与未识别清单/向量库统计）
  - [ ] 10.2 编写 `自测.py`：用生成的样例 PNG（脚本绘制 576x384 角色图等）跑通 分割 → 打标(桩) → 索引 → 查询 全链路
  - [ ] 10.3 整理 `运行说明` 到汇总报告（或 README 级说明文档，仅用户要求时）

# Task Dependencies

- [任务 2] 依赖 [任务 1]
- [任务 3] 依赖 [任务 2]
- [任务 4] 依赖 [任务 1]
- [任务 5] 依赖 [任务 1]
- [任务 6] 依赖 [任务 3] + [任务 4] + [任务 5]
- [任务 7] 依赖 [任务 6]
- [任务 8] 依赖 [任务 7]
- [任务 9] 依赖 [任务 7]
- [任务 10] 依赖 [任务 6] + [任务 7] + [任务 8]/[任务 9]
