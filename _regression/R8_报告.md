# R8 全自主科研：韵律规则回归化 + 单源漂移检测

> 轮次：R8 ｜ 日期：2026-08-31 ｜ 模式：全自主（无GPU）
> 承接 R7：本轮的离线回归集从 6→**8 套件**、断言 57→**80**，全绿。

---

## 1. 本轮新增交付

| 交付 | 套件 | 断言 | 说明 |
|---|---|---|---|
| Qwen3RapSynth 韵律规则回归 | test_x_rap_prosody.py | 14 | 音节计数/拍对齐/风格轮廓/未知风格回退/BPM钳制/get_predictor统一协议 |
| 多模态单源漂移检测 | test_x_single_source_drift.py | 8 | 静态源码级，锁定「中文标签映射/身份键/接口降级模式名」单源不变量 |
| TTS 真实现模块回归 | test_x_speech_common.py | 12 | 安装 requests 后改走**真实源码**（非契约回退），中文标签映射实测命中 |

聚合归属：`python aggregate_runner.py` → **80 通过 / 0 失败 / 8 集 / 1.14s**。

---

## 2. R8-1 · 说唱合成韵律规则离线化

`prosody_model/rules.py` 为纯规则基线（`predict_lyrics`），无需学习模型即可离线验证：
- 音节代理计数（中文1字≈1音，token字符也计入，标点排除）
- 拍对齐结构、`start_sec` 单调、空行跳过
- 三档风格轮廓（快嘴=flat / 旋律=arch / 硬核=punch）
- 未知风格回退、BPM 下限钳制、`get_predictor` 无权重时回退规则基线的统一协议

> 收益：学习型预测器替换规则基线时，本套件保证"协议不破、回退不倒"。

---

## 3. R8-2 · 单源漂移检测（真实发现）

静态源码级不变量检测，全部绿灯，并捕捉了一个**真实残留重复**：

- `TTS 中文标签映射` 仅在 `语音合成.py` 定义一次 ✅
- `AI腔身份键`（AI/助手/人工智能/模型/智能/作为一个/作为一名/机器人/程序）底座齐全 ✅
- **发现**：`情感导演解码器.py` 与底座 `base_decoding_controller.py` 的**身份键重叠率 = 1.0**——
  独立实现的第二份完整身份键表，属 R6 单源收敛后的**残余重复**，是可漂移点，标记为 R9 收敛目标。
- 接口降级三级模式名（本地/logprobs/提示）由 `接口降级.py` 唯一产出 ✅

> 该检测作为回归红线：任何新增第 3 个身份键持有者都会红灯，防止再次扩散。

---

## 4. R8-3 · TTS 真源码回归（补依赖后链路增强）

- `pip install requests` 补齐依赖后，`语音合成.py` 可真实导入。
- `标准化标签`、`应用发音纠正`、图片等比缩放等 12 条回归改写为**验证真实源码**而非契约兜底。
- 说明：回归集对重型/外部依赖做"缺则契约回退、有则真源码"双轨，保证任何环境可跑。

---

## 5. 下一轮 R9 候选

1. **收敛 `情感导演解码器` 的重复身份键表** → 复用底座常量/共享词表，消除 overlap=1.0 的两份副本（高风险改核心解码，需谨慎、先加回归再改）。
2. **扩展 `aggregate_runner` 支持子目录 glob**，纳入工作区其它目录的独立单测。
3. 对 `07_说唱合成Qwen3RapSynth/eval/metrics.py` 与 `integration/injector.py` 抽离可离线纯逻辑并补回归。

---

*生成于全自主科研模式 · 不自主停止 · 除用户外由用户中断*