# R21 全自主科研：emo_detect 词典路径 + make_schedule 自动识别回归

> 轮次：R21 ｜ 日期：2026-08-31 ｜ 模式：全自主（无GPU）
> 离线回归集：16 套件、断言 196→**202**，全绿。

---

## 1. 本轮交付

| 交付 | 套件 | 断言增量 | 说明 |
|---|---|---|---|
| 情感自动识别词典路径 + 分句自动标签 | test_x_tts_engine.py（R21 节） | +6 | `_lexicon_detect` 打分 / `detect_emotion(None)` 兜底 / `make_schedule(None)` 自动识别 |

聚合归属：`python aggregate_runner.py` → **202 通过 / 0 失败 / 16 集 / 3.55s**。

---

## 2. 覆盖明细（R21）

`06_Qwen3TTS外挂/serve/emo_detect.py`（顶层仅 stdlib + `from tts_engine import EMOTION_VOCAB`）可离线直测：

- **词典打分**：`_lexicon_detect` 对"开心/难过想哭"分别命中 `开心/悲伤`；无关键词与"平静"（不在词典）回落默认 `平静`（防误判静默情感）。
- **`detect_emotion(reply, text_chat_fn=None)`**：无 LLM 时走词典兜底，不炸且返回类别。
- **`make_schedule(text, emotions=None)`**：自动逐句识别路径走词典兜底，返回正确标签（闭环 TTS 分句自动情感注入链）。

`emo_detect` 与 `tts_engine` 共享同一 `EMOTION_VOCAB`，本测试再次验证两模块标签不漂移。

---

## 3. 累计战绩（R7→R21，16 连续轮次）

| 阶段 | 断言 | 场景 |
|---|---|---|
| R7~R11 | 57→111 | 语音/韵律/指标/TTS基座(DSP) |
| R12~R18 | 126→176 | LLM解析/模名单源/TTS基座/跨族单源/P1~P5聚合与worker |
| R19 | 176 | 聚合耗时守护 |
| R20 | 196 | EmoCompanion TTS 引擎封装 |
| R21 | 202 | 情感自动识别词典路径 |

---

## 4. 下一轮 R22 候选

1. **`_merge_refs_to_single` 多段 ICL 合并**：音频拼接/gap 插入/多情感文本拼 `[情感]原文` 纯编排（librosa 已入集，可 mock 音频为 np 数组测合并逻辑）。
2. **RVC/SVC/VITS 缺口正式评估报告落盘**：三族本地缺失已多次确认，输出一份 gap + 离线等价编排契约文档。
3. **合成通道 `synthesize_flow` meta 自洽**：分句拼接 `audio_seconds`/`n_segments` 与 schedule 一致性（mock 逐段合成回报）。

---

*生成于全自主科研模式 · 不自主停止 · 除用户外由用户中断*