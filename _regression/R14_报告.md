# R14 全自主科研：音频族 TTS 基座封装回归（Qwen3-RapSynth）

> 轮次：R14 ｜ 日期：2026-08-31 ｜ 模式：全自主（无GPU）
> 离线回归集：12→**13 套件**、断言 128→**144**，全绿。

---

## 1. 本轮交付

| 交付 | 套件 | 断言 | 说明 |
|---|---|---|---|
| TTS 基座封装回归 | test_x_rapsynth_core.py（新） | 16 | `EMOTION_VOCAB` 结构 / 兜底前缀解析 / `TTSUnavailable` 契约 / `_check()` 缺件检测 |

聚合归属：`python aggregate_runner.py` → **144 通过 / 0 失败 / 13 集 / 3.38s**。

---

## 2. 覆盖明细（R14）

`EmoCompanion_角色挂载与情感注入工程/07_说唱合成Qwen3RapSynth/tts/synthesizer.py`
顶层仅依赖 `os/time/dataclass/typing`（torch/qwen_tts 均惰性导入），因此**可直接 import 真实模块**做离线契约验证，比源码正则静态分析更进一步：

- **EMOTION_VOCAB 结构**：≥6 情感、键唯一、每个词条 `[emotion]X[/emotion]` 与键自洽。
- **兜底前缀解析**：复现 `synthesize` 首行 `VOCAB.get(emotion, VOCAB[FB])` 语义——已知情感命中词条、未知情感回退 `硬核`、`f"{前缀}{text}".strip()` 拼接语义。
- **TTSUnavailable 契约**：`RuntimeError` 子类（结构化异常，供上层转 JSON）。
- **_check() 缺件检测**：全组件就绪不抛；任缺 Base / 情感外挂 / 音色 embed 即抛且错误信息逐项列出缺件名。

---

## 3. 累计战绩（R7→R14，9 连续轮次）

| 轮次 | 断言 | 场景 | 里程碑 |
|---|---|---|---|
| R7 | 57 | 语音纯逻辑 | 聚合 runner；RVC/SVC/VITS 本地缺失确认 |
| R8 | 80 | 韵律规则/单源漂移 | 抓出情感导演身份键 overlap=1.0 |
| R9 | 94 | 指标回归/权重护栏 | 身份键同权护栏；聚合器子目录 glob |
| R10 | 105 | 注入器编排 | 移除死导入+懒导入 |
| R11 | 111 | librosa入集/TTS基座 | 指标 DSP 全链离线复核 |
| R12 | 126 | LLM 输出解析器 | RPG 打标解析契约全固化 |
| R13 | 128 | 模式名单源 | 生产者侧 return 唯一产出护栏 |
| R14 | 144 | TTS 基座封装 | 真实模块直连：结构/兜底/异常/缺件 |

---

## 4. 下一轮 R15 候选

1. **跨 TTS / RapSynth 情感标签单源**：synthesizer 的 `EMOTION_VOCAB` 键与 `语音合成.py` 中文标签映射方向的单源一致性（两族不可漂移）。
2. **ICL / x-vector 双路径分支**：`load()` 中 `ref_audio` 有无以决定路径的纯逻辑（难离线，退化为源码级不变量）。
3. RVC/SVC/VITS 缺口：确认三个本地实现缺失，评估以「打标/分句编排层」作为离线等价物。

---

*生成于全自主科研模式 · 不自主停止 · 除用户外由用户中断*