# R16 全自主科研：LLM 族 P1~P5 评测「汇总报告」聚合逻辑回归

> 轮次：R16 ｜ 日期：2026-08-31 ｜ 模式：全自主（无GPU）
> 离线回归集：13→**14 套件**、断言 147→**161**，全绿。

---

## 1. 本轮交付

| 交付 | 套件 | 断言 | 说明 |
|---|---|---|---|
| P1~P5 汇总报告纯聚合回归 | test_x_p15_aggregate.py（新） | 14 | `汇总健康度` 均值/兜底求和、`汇总裁判` win_rate、模式清单单源 |

聚合归属：`python aggregate_runner.py` → **161 通过 / 0 失败 / 14 集 / 3.42s**。

---

## 2. 覆盖明细（R16）

`KV_情感共振解码/核心/P1_5统一汇总报告.py`（顶层仅 `os/json/datetime`）评测端聚合逻辑为 **torch-free**，可直接 import 真模块离线复算：

- **模式清单单源**：7 模式常量（`裸` + P1~P5），全评测端与汇报一致。
- **汇总健康度**：每模式跨样本求平均（熵/重复/命中/长度）；**兜底总次数为求和而非平均**（关键契约）；均值 round 到 4 位；单样本 n=1 边界不炸。
- **汇总裁判**：`win_rate/胜/总` 三键透传；天然覆盖全部 7 模式。
- **相对裸百分比**：报告第 3 节 `win_rate/裸wr*100-100` 的格式化链路可复算。

### 校准

真实生成 JSON 样本结构是 `项["回复"][m]["统计"]`（`m`→`{"统计":{...}}`），测试构造据此对齐，避免用错误 schema 产生假通过。

---

## 3. 累计战绩（R7→R16，11 连续轮次）

| 轮次 | 断言 | 场景 | 里程碑 |
|---|---|---|---|
| R7 | 57 | 语音纯逻辑 | 聚合 runner；RVC/SVC/VITS 缺失确认 |
| R8 | 80 | 韵律规则/单源漂移 | 情感导演身份键 overlap=1.0 |
| R9 | 94 | 指标回归/权重护栏 | 身份键同权护栏 |
| R10 | 105 | 注入器编排 | 移除死导入+懒导入 |
| R11 | 111 | librosa入集/TTS基座 | 指标 DSP 离线复核 |
| R12 | 126 | LLM 输出解析器 | RPG 打标解析契约 |
| R13 | 128 | 模式名单源 | 生产者 return 唯一产出 |
| R14 | 144 | TTS 基座封装 | 真实模块直连回归 |
| R15 | 147 | 跨族情感单源 | 语音↔RapSynth 共享概念 |
| R16 | 161 | P1~P5 汇总聚合 | 评测端 torch-free 复算 |

---

## 4. 下一轮 R17 候选

1. **`spawn_worker` 职责隔离**：`P1_5统一生成worker.py` 若含把"注入/统计"与"推理"解耦的纯逻辑片段，抽离入集。
2. **RVC/SVC/VITS 缺口等价编排**：三族本地缺失，评估打标/分句编排层作为离线契约（低优先占位）。
3. **单源扩展**：`判定接口` 与 `混合锚点器` 的 `接口=` 关键字实参取值全集一致性（跨 KV 解码器家族规范三元组已覆盖，补全实参统一）。

---

*生成于全自主科研模式 · 不自主停止 · 除用户外由用户中断*