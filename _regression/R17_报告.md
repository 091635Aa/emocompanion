# R17 全自主科研：`接口=` 关键字实参取值「单源」护栏补全

> 轮次：R17 ｜ 日期：2026-08-31 ｜ 模式：全自主（无GPU）
> 离线回归集：14 套件、断言 161→**162**，全绿。

---

## 1. 本轮交付

| 交付 | 套件 | 断言增量 | 说明 |
|---|---|---|---|
| 接口= 实参单源护栏 | test_x_single_source_drift.py（3d） | +1 | 构造传参取值统一落在规范三元组 |

聚合归属：`python aggregate_runner.py` → **162 通过 / 0 失败 / 14 集 / 3.38s**。

---

## 2. 覆盖明细（R17）

补齐 KV 解码器家族中「`接口` 值从哪里来」的最后一块：

- **3b（消费者）**：`接口 == '…'` 比较字面量 ∈ `{本地, logprobs, 提示}`。
- **3c（生产者）**：`return '…'` 仅 `接口降级.py` 产出。
- **3d（构造实参·本轮）**：`接口="…"` 关键字实参也 ∈ 规范三元组，杜绝 `锚点解码器(..., 接口="天王老子模式")` 之类的无中生有传参；同时用"行首是否为 `接口` 赋值语句"区分真赋值（已由 3b/3c 覆盖）与本处的传参，避免重复计数。

实测：唯一传参点 `混合锚点器.py:144 接口="本地"` 合法，`self.接口 == '…'` 比较与 `self.接口 = 接口 or 判定接口(model)` 表达式形态均不被误捕。

---

## 3. 累计战绩（R7→R17，12 连续轮次）

| 轮次 | 断言 | 场景 | 里程碑 |
|---|---|---|---|
| R7 | 57 | 语音纯逻辑 | 聚合 runner；RVC/SVC/VITS 缺失确认 |
| R8 | 80 | 韵律规则/单源漂移 | 情感导演身份键 overlap=1.0 |
| R9 | 94 | 指标回归/权重护栏 | 身份键同权护栏 |
| R10 | 105 | 注入器编排 | 移除死导入+懒导入 |
| R11 | 111 | librosa入集/TTS基座 | 指标 DSP 离线复核 |
| R12 | 126 | LLM 输出解析器 | RPG 打标解析契约 |
| R13 | 128 | 模式名单源 | 生产者 return 唯一产出 |
| R14 | 144 | TTS 基座封装 | 真实模块直连回归 |
| R15 | 147 | 跨族情感单源 | 语音↔RapSynth 共享概念 |
| R16 | 161 | P1~P5 汇总聚合 | 评测端 torch-free 复算 |
| R17 | 162 | 接口实参单源 | 构造传参三元组统一 |

---

## 4. 下一轮 R18 候选

1. **`spawn_worker` 职责隔离 / `生成worker` 纯逻辑**：`P1_5统一生成worker.py` 中"注入/统计"与"推理"解耦片段抽离入集（LLM 族）。
2. **librosa 指标数值回归**：`test_x_rap_metrics.py` 已入集 f0/onset，可补 `重复率`/`相似度` 等对合成结果敏感指标的合成数据回归。
3. **全量回归耗时守护**：聚合 runner 增轮次耗时告警阈值（防套件膨胀拖垮 CI），设为纯保障项。

---

*生成于全自主科研模式 · 不自主停止 · 除用户外由用户中断*