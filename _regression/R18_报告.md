# R18 全自主科研：P1~P5 生成 worker 统计契约回归（源码白盒）

> 轮次：R18 ｜ 日期：2026-08-31 ｜ 模式：全自主（无GPU）
> 离线回归集：14→**15 套件**、断言 162→**176**，全绿。

---

## 1. 本轮交付

| 交付 | 套件 | 断言 | 说明 |
|---|---|---|---|
| 生成 worker 统计契约回归 | test_x_p15_worker.py（新） | 14 | 情感命中率/平均熵/兜底默认 行为 + 源码字面一致 |

聚合归属：`python aggregate_runner.py` → **176 通过 / 0 失败 / 15 集 / 3.44s**。

---

## 2. 覆盖明细（R18）

`P1_5统一生成worker.py` 顶层 import torch/transformers 无法离线 import，故采用**「源码字面一致 + 行为规范」双护栏**，确保"测的就是生产跑的"：

- **A1/B1 情感命中率**：空 token 列表→0.0；`命中数/总数` round(4)；`取函数体()` 抓取真实源码逐字比对，任何实现漂移即红灯。
- **A2 平均熵**：空熵列表→0.0；均值 round(4)；除零由 else 分支保障。
- **A3/B3 兜底计数默认**：解码器无 `.统计` 或缺 `触发兜底` 键→0；有值 `int()` 透传；对应源码片段字面量校验。

### 校准

初版 `取函数体()` 因 `indent.rstrip()` 为空串导致"从不 break"恒条件，误把整文件的 `main()` 抓进比对。修正为纯 `not ln.startswith(indent)` 中断，实现真正的函数边界提取。

---

## 3. 累计战绩（R7→R18，13 连续轮次）

| 轮次 | 断言 | 场景 | 里程碑 |
|---|---|---|---|
| R7 | 57 | 语音纯逻辑 | 聚合 runner；RVC/SVC/VITS 缺失确认 |
| R8 | 80 | 韵律规则/单源漂移 | 情感导演身份键 overlap=1.0 |
| R9 | 94 | 指标回归/权重护栏 | 身份键同权护栏 |
| R10 | 105 | 注入器编排 | 移除死导入+懒导入 |
| R11 | 111 | librosa入集/TTS基座 | 指标 DSP 离线复核 |
| R12 | 126 | LLM 输出解析器 | RPG 打标解析契约 |
| R13 | 128 | 模式名单源 | 生产者 return 唯一产出 |
| R14 | 144 | TTS 基座封装 | 真实模块直连回归 |
| R15 | 147 | 跨族情感单源 | 语音↔RapSynth 共享概念 |
| R16 | 161 | P1~P5 汇总聚合 | 评测端 torch-free 复算 |
| R17 | 162 | 接口实参单源 | 构造传参三元组统一 |
| R18 | 176 | 生成worker契约 | 源码白盒：一致+行为双护栏 |

---

## 4. 下一轮 R19 候选

1. **RVC/SVC/VITS 缺口正式评估报告**：三个本地实现缺失已确认，出一份 gap + 离线等价编排方案（打标/分句契约兜底；真算法等效留待 GPU 轮）。
2. **librosa 合成敏感指标补全**：`相似度`/`重复率` 对合成降噪曲线/节拍序列的数值回归。
3. **聚合耗时守护**：runner 增单套件耗时告警（防套件膨胀拖垮 CI）。

---

*生成于全自主科研模式 · 不自主停止 · 除用户外由用户中断*