# 任务清单（修复实时通话：AI 用定制音色打电话）

## 任务概览

- [x] 任务 1：修复 `response.audio.delta` 音频字段（delta），使 AI 回复出声
- [x] 任务 2：定制音色通话可用性按 target_model 复核，防误用非实时绑定音色
- [x] 任务 3：端到端验证（协议往返 + 页面 JS 冒烟 + 清理诊断脚本）

## 任务详情

### 任务 1：修复 AI 回复音频播放
- [x] 子任务 1.1：在 [通话控制.js](file:///f:/缘圆—Ai智能体/缘圆智能体/前端页面/脚本/通话控制.js) 的 `response.audio.delta` 分支，将 `事件.audio` 改为 `事件.delta || 事件.audio`（官方字段为 delta）
- [x] 子任务 1.2：确认 `解码Base64` 输出 ArrayBuffer，`播放音频` 以 24kHz 解码播放链路不变
- [x] 子任务 1.3：验证：`response.audio.delta` 到达即触发 `播放音频`，不再因字段缺失静默丢弃

### 任务 2：定制音色通话可用性按 target_model 复核
- [x] 子任务 2.1：`核心模块/语音合成.py` 的 `查询在线音色()` 输出每个音色携带 `target_model`（来自 voice_list）
- [x] 子任务 2.2：`核心模块/音色管理.py` 的 `全部音色()`：构建 在线音色 target_model 映射；本地 omni 复刻音色（`qwen-omni-vc-*`）若有在线记录，按"绑定模型 ∈ 实时模型集合"复核 `通话可用`；在线复刻音色同样按绑定模型标注 `通话可用`
- [x] 子任务 2.3：验证：`qwen-omni-vc-yuanyuan-voice-20260802030956459-59e8`（绑定 qwen3.5-omni-plus-realtime）通话可用=True；绑定 `qwen3.5-omni-flash` 的同前缀音色通话可用=False

### 任务 3：端到端验证与清理
- [x] 子任务 3.1：用 [诊断通话.py](file:///f:/缘圆—Ai智能体/诊断通话.py)（临时脚本）复核：定制音色会话往返产生 `response.audio.delta`（字段 delta）且数量 > 0
- [x] 子任务 3.2：启动应用，浏览器打开通话页，确认页面无 JS 错误、音色面板可加载、定制音色自动选中
- [x] 子任务 3.3：删除临时诊断脚本 `诊断通话.py`（诊断完成后清理）

## 任务依赖

- [任务 2] 依赖 [任务 1]（无强依赖，可并行）
- [任务 3] 依赖 [任务 1] 与 [任务 2]

## 并行建议

- 任务 1、任务 2 可并行推进
