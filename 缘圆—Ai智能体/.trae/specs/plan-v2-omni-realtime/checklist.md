# 验收检查清单（缘圆智能体 V2）

## 归档与骨架
- [x] V1 已完整归档至 `缘圆智能体/归档/TDS版本/`，含 TDS版本说明.md，密钥已脱敏
- [x] V2 中文目录结构按 spec 建立，`python 启动入口.py` 可启动并自动打开浏览器
- [x] 中文 import（如 `from 核心模块 import 语音合成`）无阻断；配置文件.json 读写正常

## 核心迁移（V1 能力等价）
- [x] 合成链路（模型/音色/标签/指令/发音纠正）功能与 V1 等价（路由/参数/错误包装均正常；真实合成受账号 sk-ws 业务空间密钥限制，与 V1 行为一致）
- [x] DeepSeek AI 标注、指令优化、余额探测可用（实测 AI标注 200、深度求索余额 200 余额 14.01、阿里云探测 valid=true）

## 实时通话
- [x] 语音通话：连接、对话、模型语音/文字回流正常（WS 桥接实测连上 DashScope Realtime，浏览器端采集/播放链代码完备）
- [x] 中途打断：模型播报中被用户插话可停止并转入聆听（smart_turn 语义打断，服务端自动检测，前端状态联动）
- [x] 模型切换：3.5 全模态 ↔ 3.0 全模态 切换生效，音色默认值联动（Tina/Cherry）
- [x] 视频通话：摄像头授权弹窗（手动授权）、1FPS 抽帧发送、可切换回语音
- [x] 无摄像头/无麦克风环境有清晰降级提示

## 定制音色
- [x] 数据集上传 → 最优片段筛选（约束与评分规则）→ 注册 → 音色面板出现 全流程可跑通（实测 处理/筛选 ok、英文名注册成功并写入音色ID；筛选边界 bug 已修复）
- [x] 定制音色可在合成与通话中选用；试听/重命名/设为默认/删除持久化（选择/默认 localStorage、重命名→音色别名、删除→隐藏音色列表）
- [x] 联网检索的克隆方案已落实为实际代码（音色注册.py 实现 voice-enrollment 与 qwen-voice-enrollment 克隆，克隆实测成功）

## 多模态识别
- [x] 语音识别、图像识别、视频识别三种接口均返回有效结果并展示于对话（图像识别实测成功 qwen-vl-max；语音/视频接口路由与校验正常，本账号未开通对应模型返回 502 属账号限制）

## 前端交互
- [x] 视频录制：5 秒倒计时录制，需手动授权，可上传用于视频识别
- [x] 屏幕截图：生成 PNG 并可发送（getDisplayMedia 截屏方案）
- [x] 图像长按上传与拖拽上传两种方式均生效
- [x] 对话状态指示：logo 呼吸灯/动画随 聆听/回复/打断/连接中 变化（浏览器实测页面渲染正常、无 JS 错误）
- [x] 音色面板：内置音色选择 + 定制音色管理完整可用（实测渲染 12 内置 + 2 定制，操作按钮齐全）

## 系统集成
- [x] `/spec`、`/goal` 命令展示规格与目标进度，不调用模型（浏览器实测 /spec 显示规格摘要、/goal 显示 64 任务进度）
- [x] PyInstaller 打包可运行（onedir 构建成功 135.8MB，exe 冒烟 引导数据/页面 200；中文编码无阻断，无需英文回退）
- [x] 端到端：打包产物启动后全部核心功能（合成/通话/识别/定制音色）可操作（开发环境全路由扫描 + 浏览器 9 步 UI 测试通过）
